# contextcompiler
ContextCompiler intelligently extracts and summarizes the essential context an LLM needs to understand and modify your code — types, APIs, architecture, conventions — without flooding the context window.
