# ccc-workspace.yml — Auto-generated by `ccc workspace init`
# Review and fill in the TODOs before committing.

name: ccc
version: 1

services:

  ccc:
    path: ./ccc
    type: library
    # languages: python
    tags:
      - core  # TODO: add meaningful tags
    # depends_on:  # TODO: add dependencies if any
    #   - other-service
    description: "python library"

  tests:
    path: ./tests
    type: backend-api
    # languages: python, typescript
    tags:
      - auth
      - users
      - search
      - infra
    # depends_on:  # TODO: add dependencies if any
    #   - other-service
    description: "python/typescript backend api"

# ── Notes ──────────────────────────────────────────────────────────────
# Tags are used by: ccc workspace query --tags <tag>
# depends_on drives the change-sequence ordering
# Run `ccc` in each service directory to generate .llm-context/ first
# Then run `ccc workspace generate` to build the cross-repo overview
