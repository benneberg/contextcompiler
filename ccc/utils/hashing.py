"""Hashing and fingerprinting utilities."""
from pathlib import Path
import hashlib

def hash_file_quick(path: Path) -> str:
    """Generate quick hash of file."""
    # ... (move from main file)

def compute_fingerprint(root: Path, key_files: list[str]) -> str:
    """Compute project fingerprint."""
    # ... (move from main file)
