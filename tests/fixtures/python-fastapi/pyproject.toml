[project]
name = "test-fastapi-app"
version = "0.1.0"
description = "Test FastAPI application for LLM context generator"
requires-python = ">=3.10"

[project.dependencies]
fastapi = ">=0.104.1"
uvicorn = ">=0.24.0"
pydantic = ">=2.5.0"
sqlalchemy = ">=2.0.23"
